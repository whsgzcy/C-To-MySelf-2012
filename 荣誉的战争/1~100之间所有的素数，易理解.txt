#include <stdio.h>//输出1~100之间所有的素数

void main()
{
	int i,j;
	for(i = 2;i <= 100;i++)//此时的循环条件所对应的数字是除一以外的
	{
		for(j = 2;j < i;j++)
		if(i % j == 0) 
		break;//此时若到判断六是不是素数那么是比较1,2,3,4,5,6则2,3,6符合
		if(j == i)//根据判断素数的要求则和i相等的符合条件
		printf("%3d",i);
	}
}
